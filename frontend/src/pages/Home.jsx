import React from 'react'
import { useUser } from '../context/user.context'

const Home = () => {

  const {user}=useUser();
  return (
    <section className='h-full'>

    
    </section>
  )
}

export default Home;